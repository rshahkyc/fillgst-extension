{
  "name": "fillgst-extension",
  "version": "0.1.0",
  "private": true,
  "description": "FillGST Helper — Chrome extension for GST portal automation per user PC",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@crxjs/vite-plugin": "^2.0.0-beta.32",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
