{
  "name": "fillgst-extension",
  "version": "0.7.9",
  "private": true,
  "description": "FillGST Helper — Chrome extension for GST portal automation per user PC",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:crx": "vite build && node scripts/build-crx.mjs",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.32",
    "@types/chrome": "^0.0.287",
    "crx3": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
